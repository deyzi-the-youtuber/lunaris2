#include <common.h>

int loglevel_check(const char fmt[])
{
  // Checks if a string can be printed.
  // loglevel allows certain kernel messages
  // to be printed to the screen or to the
  // terminal.
}
